отдельный настоящий FloorToIntStrict() при необходимости лучше сделать позже отдельно, с явной семантикой;
пока не надо тихо подменять одну семантику другой.

Если хочешь, на следующем шаге можем явно добавить:

TruncToInt()
FloorToInt()
и развести их по контракту.



И сразу оговорка:
здесь мы делаем строгий и безопасный v1 без лишней магии:

deterministic integer sqrt
Length
Distance
NormalizeApprox

Без float, без SIMD, без “умных” эвристик.


